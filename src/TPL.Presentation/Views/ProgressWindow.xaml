<Window x:Class="TPL.Presentation.Views.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:TPL.Presentation.Controls"
        Title="Progress"
        Width="420" Height="130"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True">
    <Border Background="#181A1F" BorderBrush="#333640" BorderThickness="1" CornerRadius="10">
        <Grid Margin="16,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock x:Name="txtTitle" Text="{Binding Title}" Foreground="#2563EB" FontSize="13" FontWeight="Bold" FontFamily="Segoe UI" Margin="0,0,0,10"/>
            
            <controls:DarkProgressControl x:Name="pnlProgress" Grid.Row="1" Margin="0,0,0,8"/>

			<TextBlock x:Name="txtSubTitle" Text="{Binding SubLabel}" Foreground="#6B7280" FontSize="11" FontFamily="Segoe UI" Grid.Row="2"/>
        </Grid>
    </Border>
</Window>
