<UserControl x:Class="TPL.PdfToolsDashboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <Style x:Key="ToolCardButton" TargetType="Button">
            <Setter Property="Height" Value="142"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="Foreground" Value="#E8EAED"/>
            <Setter Property="Background" Value="#222C38"/>
            <Setter Property="BorderBrush" Value="#414A55"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CardBorder"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CardBorder" Property="Background" Value="#293747"/>
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="#2563EB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="CardBorder" Property="Background" Value="#1E2B3A"/>
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="#60A5FA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="#60A5FA"/>
                                <Setter TargetName="CardBorder" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  Padding="22,16,22,20">
        <StackPanel>
            <TextBlock Text="PDF tools"
                       Foreground="#E8EAED"
                       FontSize="24"
                       FontWeight="SemiBold"/>
            <TextBlock Text="Choose a tool to open and work with your PDF files."
                       Margin="0,5,0,14"
                       Foreground="#8A93A1"
                       FontSize="12.5"/>

            <ItemsControl ItemsSource="{Binding Tools, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="4"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource ToolCardButton}"
                                Click="ToolCard_Click"
                                ToolTip="{Binding Description}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Border Width="48" Height="48"
                                        HorizontalAlignment="Center"
                                        Background="#1F2563EB"
                                        BorderBrush="#552563EB"
                                        BorderThickness="1"
                                        CornerRadius="12">
                                    <TextBlock Text="{Binding Glyph}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Foreground="#93C5FD"
                                               FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                               FontSize="26"/>
                                </Border>

                                <TextBlock Grid.Row="1"
                                           Margin="0,10,0,0"
                                           HorizontalAlignment="Center"
                                           Foreground="#E8EAED"
                                           FontSize="13"
                                           FontWeight="SemiBold"
                                           Text="{Binding Title}"/>
                                <TextBlock Grid.Row="2"
                                           Margin="4,4,4,0"
                                           HorizontalAlignment="Center"
                                           Foreground="#8A93A1"
                                           FontSize="10.5"
                                           Text="{Binding Caption}"
                                           TextAlignment="Center"
                                           TextTrimming="CharacterEllipsis"/>
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border Margin="6,12,6,0"
                    Padding="12,9"
                    Background="#1D232C"
                    BorderBrush="#333640"
                    BorderThickness="1"
                    CornerRadius="6">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE8B7;"
                               Foreground="#60A5FA"
                               FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                               FontSize="15"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="You can also drag and drop PDF files anywhere in this window."
                               Margin="9,0,0,0"
                               Foreground="#8A93A1"
                               FontSize="11.5"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
